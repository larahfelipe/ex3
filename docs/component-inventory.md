# Inventário de componentes reutilizáveis — TASK 0.3

Classificação do frontend (`web/src`) no commit `8dc9edd`, para decidir o que preservar antes das FASES 8–13. 4.481 linhas de TS/TSX.

## UI primitives — `src/components/ui`

shadcn/ui sobre Radix, com `cn()` (`clsx` + `tailwind-merge`) e `class-variance-authority`. Base sólida, mantida.

| Componente | Base | Avaliação |
| --- | --- | --- |
| `button` | Radix Slot + CVA | **Preservar.** Variantes já cobrem o novo produto. `outline` tem fundo transparente, como os campos, e herda a superfície em que está. O toque encolhe o botão a 98% sob `motion-safe:`, como os itens do menu lateral, e confirma o clique |
| `card` | — | **Preservar.** Base dos KPI cards da FASE 8. `CardTitle` e `CardDescription` saíram na auditoria de UX de 2026-09-22: todo título de seção vem de `SectionHeader`, com `<h2>`. Elevação em três níveis: o fundo da página, o card (`bg-surface`, borda e `shadow-surface`, uma sombra quase só perceptível no tema claro, onde superfície e fundo são o mesmo branco e a borda é o que recorta o card) e as sobreposições (`bg-surface-elevated` e `shadow-elevated`: diálogo, popover, menu e lista do `Select`). O card tem `rounded-2xl`, o mesmo do diálogo a partir de `sm`, e contêineres dentro dele, `rounded-xl`; o preenchimento é 20 px no celular e 24 px a partir de `sm`. Nenhuma tela sobrescreve a elevação do card, e nenhum card reage ao ponteiro, porque nenhum é clicável inteiro: a interação fica nos botões, links e linhas de tabela dentro dele |
| `dialog`, `alert-dialog` | Radix | **Preservar.** Focus trap e `aria` vêm do Radix. Desde a TASK 15.4 o conteúdo é limitado a `max-h-dvh` e rola por dentro — abaixo de `sm` isso equivale à tela integral —, e o rodapé separa os botões empilhados com `gap-2`. Os dois compartilham `DIALOG_OVERLAY_CLASS` e `DIALOG_CONTENT_CLASS`, de `dialog.tsx`: o conteúdo entra e sai no lugar, com fade e escala de 95%, em 200 ms com desaceleração na abertura e 150 ms com aceleração no fechamento, sem deslize — o `transform` da animação se soma ao `translate` que centraliza o conteúdo, e o deslize antigo o fazia entrar pela diagonal. O overlay escurece a 60% e desfoca a página atrás (`backdrop-blur-sm`); sob `prefers-reduced-transparency`, variante `reduced-transparency` de `globals.css`, volta a 80% e sem desfoque |
| `dropdown-menu`, `select` | Radix | **Preservar.** Reduzidos na auditoria de UX de 2026-09-22 às partes com consumidor: `DropdownMenu`, `Trigger`, `Content`, `Item` e `Separator`, que o menu da tabela de posições trouxe de volta para isolar a exclusão; `Select`, `Trigger`, `Value`, `Content` e `Item`, com os botões de rolagem que o próprio `SelectContent` usa. `SelectTrigger` segue o `Input`: fundo transparente, borda `--input` e `aria-invalid:border-negative`; o placeholder fica em `--muted-foreground` por `data-placeholder:`, já que `placeholder:` não se aplica a botão |
| `input`, `label` | Radix | **Preservar.** `checkbox` foi removido com `@radix-ui/react-checkbox` na auditoria de dependências (TASK 20.3): estava sem consumidor desde que a tabela de ativos deixou de selecionar linhas. `Input` e o textarea de notas têm fundo transparente, que herda a superfície do card ou do diálogo, texto de 16 px abaixo de `md` — o iOS amplia a página ao focar campo com fonte menor — e 14 px a partir dele. O ícone à esquerda não recebe clique, que chega ao campo; o botão de mostrar a senha é um quadrado de 32 px recuado 4 px da borda, em vez de cobri-la no hover |
| `popover`, `calendar` | Radix, react-day-picker 10 | **Novos.** `Popover` sobre `@radix-ui/react-popover`, com os tokens de superfície elevada do `dropdown-menu`. `Calendar` é o da shadcn/ui sobre `DayPicker`, com as classes do projeto nos elementos que a v10 nomeia (as mesmas da v9), setas do `lucide-react` e dias como botões `ghost` |
| `segmented-control` | — | **Novo.** `SegmentedControl`/`SegmentedControlItem` estiliza um grupo de `input[type=radio]` nativo como um controle segmentado — nenhum papel ARIA próprio além do que o HTML já dá ao rádio. O item marcado usa `--primary`/`--primary-foreground`, e o hover só escurece o texto do item não marcado (`peer-not-checked:hover:`): `hover:text-foreground` vem depois de `peer-checked:` na folha, com a mesma especificidade, e trocava o texto do marcado pela cor do primeiro plano sobre `--primary`; o contêiner ganha `border-negative` quando o `ChoiceField` que o envolve está inválido (`in-data-invalid:`). O fundo do item marcado é um indicador só, compartilhado pelos itens do controle (`layoutId` num `LayoutGroup` com o `useId` do controle, para dois controles na mesma tela não trocarem indicador), que desliza até a opção escolhida numa mola de 300 ms com leve rebote (`TAB_SPRING`), enquanto o texto troca de cor no mesmo tempo. O controle lê o rádio marcado do DOM, por `useSyncExternalStore` sobre o `change` do grupo, e não das props: o rádio registrado no react-hook-form não é controlado, o clique só dispara `change` no rádio que ficou marcado, e o `reset` marca outro sem evento algum. Até essa leitura, no servidor e na primeira renderização, o fundo vem do CSS (`peer-checked:bg-primary`), e a hidratação não pisca. Cinco consumidores: tipo da transação (`transaction-form-dialog.tsx`), moeda base da carteira (`portfolio-form-dialog.tsx`) e do sign-up (`sign-up-form.tsx`), registrados no formulário, e, controlados, a visão da alocação (`allocation-chart.tsx`) e o período do desempenho (`performance-chart.tsx`) |
| `table` | HTML semântico | **Preservar como primitive**, mas não cobre ordenação, seleção ou estado vazio — o consumidor implementa tudo. Desde a TASK 15.3 o wrapper é um `section` que exige a prop `label`: enquanto transborda, vira região nomeada e focável; `regionClassName` ajusta a caixa que rola. `TableRowHeader` é a primeira célula de cada linha, um `th scope="row"` com o estilo de `TableCell`, para o leitor de tela anunciar o ativo, o grupo ou a data ao percorrer as colunas. Toda tabela tem `TableCaption` `sr-only` com o que ela lista |
| `pagination` | — | **Removido** na auditoria de UX de 2026-09-22: nunca teve consumidor, e toda listagem pagina por botões, não por link |
| `skeleton` | — | **Preservar.** Vira base do `LoadingState` (TASK 12.4) |
| `separator`, `tooltip` | Radix | **Removidos** na TASK 20.3, com `@radix-ui/react-separator` e `@radix-ui/react-tooltip`: nenhuma tela os renderizava |
| `sonner` | Sonner | **Preservar** |

**Ausentes e necessários:** `tabs`, `sheet` (nav mobile, TASK 13.2), `badge`, `chart`. Nenhuma biblioteca de gráficos está instalada — decisão pendente para a TASK 8.3.

## Layout components

| Componente | Avaliação |
| --- | --- |
| `components/sidebar.tsx` | **Redesenhado.** `nav[aria-label="Main"]` único, que muda de barra inferior fixa (< `sm`) para trilha lateral fixa à esquerda (≥ `sm`), colapsável para ícone só a partir de `lg` — abaixo disso não há espaço horizontal para uma trilha estreita e o rótulo compacto junto do ícone. Duas seções: `PORTFOLIO_SECTIONS` (Overview, Assets, Portfolios, numa lista sem rótulo, separada da conta pela borda e pelo espaço até o rodapé da trilha) e `ACCOUNT_SECTION` (Account, com o nome do usuário como `detail` quando expandida, e Sign out abaixo, fora da rota — some abaixo de `sm`, onde a barra é só navegação e a conta se alcança pelo próprio item Account). Item ativo por `usePathname`, também nas sub-rotas (`isCurrentPath`), com `aria-current="page"`, indicador lateral (`bg-primary`) e ícone destacado só quando expandida. Estado colapsado/expandido é lido do cookie `APP_STORAGE_KEYS.Navigation` no server component (`(protected)/layout.tsx`, via `cookies()`) e passado como `initialState`: a primeira renderização já sai no estado certo, sem flash. O toggle é um círculo de 24 px sobre a borda direita da trilha, centrado na linha da marca, com um chevron que aponta para onde a trilha vai — à esquerda expandida, gira para a direita colapsada — e alvo de 44 px por um pseudo-elemento; no hover, a borda da trilha acende com ele (`has-[>button:hover]`). Para ele poder atravessar a borda, `nav` não rola: o conteúdo rola num contêiner interno, que é `contents` na barra inferior. `aria-label` e `title` dinâmicos ("Collapse navigation"/"Expand navigation"); grava o cookie a cada troca (`max-age` de um ano) e só aparece em `lg`; a classe `navigation-expanded:` (variante de `data-navigation-state` em `globals.css`) condiciona rótulo, indicador e fundo do item ativo, sem duplicar o layout entre os dois modos. Toda cor vem de token (`--primary`, `--accent`, `--muted-foreground`, `--negative` no Sign out); nenhuma cor fixa |
| `app/(protected)/layout.tsx` | **Preservar** como shell: link de pulo, `Sidebar` e `<main>`. O contêiner dentro do `<main>` dá a toda página o padding — lateral de 16, 24, 32 e 40 px na base, em `sm`, `lg` e `xl`, e vertical de 32 px — e para em 1536 px, centralizado; cada página só empilha as seções com `space-y-6` |
| `app/(public)/layout.tsx` | **Preservar.** Formulário à esquerda e arte à direita a partir de `lg`. O layout dá às duas telas uma coluna única de até 400 px, sem rodapé, centralizada na vertical e na horizontal na área do formulário (a tela inteira abaixo de `lg`, a metade esquerda a partir dele) por margens automáticas. Quando a coluna não cabe na altura, as margens caem a zero e a página rola, sem cortar o topo. Uma mudança de altura, como erro de validação, aviso da API ou troca de etapa do sign-up, recentraliza a coluna, e a marca e o título se movem metade da diferença; ela entra com fade e 8 px de subida em 500 ms; a arte se funde ao fundo por um gradiente de 96 px na borda interna. `_components/public-page-header.tsx` é o cabeçalho das duas: a marca em `font-display`, um fio em gradiente de `--primary` que retoma as luzes da arte, e o `<h1>` da tela em destaque, sem descrição. `_components/auth-notice.tsx` é o aviso das duas, com ícone, em tom `warning` (sessão expirada) ou `negative` (erro da API, `role="alert"`). `_components/auth-alternative.tsx` fecha as duas com a pergunta e o link para a outra tela, sublinhado em `--muted-foreground` que passa a `--foreground` no hover. `_components/session-cache-disposal.tsx` descarta o cache do React Query quando o layout monta, depois que as telas protegidas desmontaram |
| `app/layout.tsx` | **Preservar** — fontes, tema, providers |

**Ausentes:** `PageHeader`, `SectionHeader` (TASK 12.3), navegação mobile (TASK 13.2).

**Fronteira Server ↔ Client (TASK 16.2).** `'use client'` marca o que precisa de estado, efeito, contexto ou evento — e nada além disso. São client: os providers, a navegação (`usePathname`), as três telas que leem dados por hook, os dois formulários de autenticação e os primitives que embrulham Radix ou guardam estado, como `table.tsx` desde que a região rolável observa a própria caixa. São server: `app/layout.tsx`, os dois layouts de grupo, as páginas de sign-in e sign-up — que renderizam só a moldura estática em volta do formulário client — e todo Route Handler de `app/api/v1`. Um componente sem interação que só é usado dentro de um client component continua no grafo do cliente, e marcá-lo não muda nada: a fronteira é definida por quem o renderiza.

## Feature components — `app/(protected)/(overview)/_components`

A Overview é a página principal, em `/`. O redirect permanente de `/` para `/assets` saiu de `next.config.js`; sign-in, sign-up e o acesso autenticado a uma rota pública levam a `/`. Navegador que guardou o 308 antigo segue indo para `/assets` até limpar o cache. A página lê a carteira ativa e mostra o nome e a moeda base no cabeçalho, com estado de carregando, erro com nova tentativa e carteira ausente.

| Componente | Responsabilidade |
| --- | --- |
| `(overview)/page.tsx` | Cabeçalho, com "Add asset", e composição das seções: valor da carteira, performance, alocação ao lado das posições em telas largas, e transações recentes. Adicionar ativo — pelo cabeçalho ou pelo vazio das posições — abre `AssetDialogs` na própria Overview |
| `portfolio-value-card.tsx` | `PortfolioValueCard`, sobre `QuerySection`, com `GET /v1/portfolio/overview` numa lista de definição: valor total em destaque, variação do dia, valor investido e lucro ou prejuízo, com percentual, e o horário das cotações (`quotedAt`) em `<time>`. Vazio quando a carteira não tem posição com unidades: `totalValue` `0` sem `quotedAt`. Desatualizado pelo aviso de `QuerySection`. Cotação de reserva do provedor aparece só pelo horário antigo. "Refresh", no cabeçalho, é `RefreshPortfolioButton` |
| `allocation-chart.tsx` | `AllocationChart`, sobre `QuerySection`, com `GET /v1/portfolio/allocation`: seletor por classe (`byType`) ou por ativo (`byAsset`) num `SegmentedControl` no cabeçalho, anel em SVG decorativo (`aria-hidden`) e legenda em tabela, a alternativa textual, com cor, nome, percentual e valor na moeda base. Grupos na ordem da API; grupo sem `allocation` fica fora do anel e aparece como "Not available". Anel e legenda lado a lado quando o card tem ao menos 28rem (container query), empilhados abaixo disso. As cores vêm de uma paleta de 10 e se repetem a partir do 11º grupo (TD-025). Trocar a visão desliza anel e legenda por `SlideTransition` no sentido da opção escolhida — Asset fica à direita de Class, então a visão por ativo entra pela direita —, e o card acompanha a altura da nova legenda |
| `positions-summary.tsx` | Posições em tabela, 10 por página, com a página em `positionsPage` na URL e a página anterior exibida enquanto a próxima carrega; o símbolo leva ao detalhe do ativo; sem posições, abre o diálogo de adicionar ativo da própria Overview |
| `recent-transactions.tsx` | As 5 transações mais recentes em `TransactionsTable`: a primeira página da listagem, que ordena por `executedAt` decrescente |

## Componentes compartilhados — `src/components`

| Componente | Responsabilidade |
| --- | --- |
| `amounts.tsx` | `Amount`, `SignedAmount` e `UnavailableValue`: valor ausente aparece como `-` e é lido como "Not available" |
| `load-error-alert.tsx` | `LoadErrorAlert`, o erro com nova tentativa (`role="alert"`), usado pelas seções e pelas páginas da Overview, da tela de ativos e do detalhe do ativo |
| `query-section.tsx` | `QuerySection` enquadra a seção num card com título `h2` e resolve, a partir da query, carregando (`aria-busy`), erro com nova tentativa, vazio e conteúdo; o dado em cache segue exibido se a nova busca falhar, abaixo do `StaleState` com nova tentativa (`isRefetchError`). O erro com nova tentativa é o `LoadErrorAlert` compartilhado. Quando o conteúdo exibido troca — outra página de dados ou o estado vazio — e o foco, que estava na seção ou num diálogo aberto a partir dela, cai no `body`, o foco vai para o `<h2>`, que `SectionHeader` deixa focável por script (`tabIndex={-1}`). O conteúdo entra com `APPEAR_CLASS` quando substitui o carregamento ou o vazio; nova página e refetch não repetem o fade, porque o contêiner continua montado |
| `refresh-portfolio-button.tsx` | `RefreshPortfolioButton`, o "Refresh" do valor da carteira, na Overview, e das posições. Invalida o escopo inteiro da carteira por `useRefreshPortfolio`, para que nenhum número da tela saia de uma cotação mais nova que os outros, com `cancelRefetch: false`: junta-se às buscas que já estão correndo em vez de reiniciá-las. Fica `aria-disabled`, com o ícone girando, até todas as buscas terminarem, e ignora os cliques até lá, então clique repetido não gera requisição. A falha aparece na seção que falhou, pelo `StaleState` ou pelo `ErrorState` de `QuerySection`. O backend guarda cada cotação por 60 s, então o botão não leva ao provedor mais de uma busca por minuto por instrumento |
| `page-navigation.tsx` | `PageNavigation`, o `nav` de "Previous", posição e "Next" das quatro listagens paginadas — posições na Overview e na tela de ativos, transações do ativo e carteiras —, com a posição em `aria-live="polite"` e um `detail` opcional, a contagem de posições na tela de ativos. Os botões dos extremos usam `aria-disabled`, não `disabled`, para que chegar à primeira ou à última página não tire o foco do botão |
| `submit-button.tsx` | `SubmitButton`, o submit de todo formulário, com spinner enquanto envia. Pendente, fica `aria-disabled` e cancela o próprio clique, o que cancela também a submissão implícita por Enter num campo; `disabled` tiraria o foco do botão e o deixaria no `body` quando o envio falha. `disabled` segue para indisponibilidade que não vem do envio, como edição sem alteração |
| `toaster.tsx` | `Toaster`, o `sonner` do app, no canto inferior direito. Desenha no tema de `APP_THEME` (`common/constants.ts`), a mesma classe que `app/layout.tsx` põe no `<html>`, com as cores dos tokens de superfície elevada, então segue o tema da aplicação. O CSS do `sonner` entra sem camada e depois do app, então as classes que o sobrepõem levam o modificador `!`: sem ele, o ícone de 22 px, o alinhamento ao topo e a sombra não apareciam. Cada toast tem um botão de fechar no canto superior direito, com anel de foco em `--focus`, como a ação. A única borda no tom do status é a inferior, e ela conta o tempo: uma barra de 3 px no tom — `--positive`, `--negative`, `--warning`, `--info` — esvazia sobre um trilho do mesmo tom a 25% durante a vida do toast, em `globals.css`, no lugar da borda neutra de baixo. Toast `loading`, que não tem prazo, mantém a borda neutra. A animação dura `--toast-lifetime`, que `toastLifetimeOf` (`lib/toast-lifetime.ts`) grava junto da `duration`, e para sempre que o `sonner` segura o timer — lista expandida pelo ponteiro ou pelo atalho (`data-expanded`), pressionada (`:active`) ou documento oculto (`data-document-hidden`, posto pelo `Toaster`, que resolve o estilo na hora porque documento oculto não renderiza). Com `prefers-reduced-motion`, ela mantém a duração, como o spinner, porque diz quanto tempo o toast fica. Toast saindo por swipe devolve a animação ao `sonner` |
| `motion-scope.tsx` | `MotionScope`, o `LazyMotion` (`strict`) e o `MotionConfig reducedMotion="user"` em volta de cada primitiva animada, em vez do app: rota que não anima não baixa nada do framer-motion, e o pacote de recursos (`lib/motion-features.ts`) chega por `import()` na primeira vez que uma primitiva monta. `strict` recusa o `motion` completo, que traria os recursos junto do componente. Sob `prefers-reduced-motion`, transform e layout não animam; opacidade, sim |
| `slide-transition.tsx` | `SlideTransition`, a troca de painel das abas: o painel que entra desliza no sentido de `direction` (`1` pela direita, `-1` pela esquerda) com a mola de `TAB_SPRING` e o que sai desliza para o lado oposto, os dois sobrepostos (`AnimatePresence mode="popLayout"`). O que sai fica `inert` — nem foco nem leitor de tela chegam ao conteúdo que está indo embora. A altura do contêiner é medida por `ResizeObserver` e animada sem rebote, sem transição sob `prefers-reduced-motion`; o recorte (`overflow-hidden`) tem 6 px de folga em cada lado (`-m-1.5`/`p-1.5`), para o anel de foco dos campos não ser cortado. Dois consumidores: `AllocationChart` e `SignUpForm` |
| `info-note.tsx` | `InfoNote`, a nota auxiliar: texto em `--muted-foreground` com o ícone `Info` alinhado à primeira linha. É para a regra ou a consequência que se lê antes de agir, sem o peso de um aviso: a carteira única que não se exclui, no rodapé da lista de carteiras, e o sign-out que a troca de senha causa, junto do botão. Erro e aviso continuam em caixa com borda e tom, como `AuthNotice` |
| `info-tip.tsx` | `InfoTip`, um botão de 24 px com o ícone `Info` que abre um `Popover` com a explicação, rotulado `About …` no botão e no conteúdo. Abre por clique, toque ou teclado, não por hover, porque tooltip de hover não aparece em tela de toque. Serve a termo ou conta que o rótulo não explica: o retorno ponderado no tempo de `PerformanceChart` e o `yield on cost` do detalhe do ativo, este pela prop `info` de `Metric`. Não repete o que já está escrito na tela |
| `performance-chart.tsx` | `PerformanceChart`, sobre `QuerySection`, com `GET /v1/portfolio/performance`, da carteira inteira na Overview e de uma posição, por `symbol`, no detalhe do ativo: seletor de período em botões de rádio nativos no cabeçalho, de `1W` a `MAX`, guardado em `range` na URL, com o período anterior exibido enquanto o novo carrega; o seletor só aparece quando há o que filtrar — uma série no período, ou, se ele veio vazio, no histórico inteiro (`MAX`), pedido só nesse caso —, e continua quando o período falha, para outro ser escolhido; linha em SVG decorativo (`aria-hidden`) de `SeriesChart`, com área sob ela, e a série inteira em tabela dentro de um `<details>`, a alternativa textual, com dia, valor, investido, aporte líquido e retorno. O ponteiro move o marcador, a leitura de dia, valor e retorno do gráfico e a do cabeçalho; sem ponteiro, o cabeçalho lê o último ponto. Nenhum valor é calculado no web: os números viram coordenadas apenas para desenhar, e todo valor exibido é formatado da string decimal da API. O dia da série é formatado em UTC, o fuso em que ele fecha. `1D` fica fora do seletor (TD-032). Enquanto o novo período carrega, o anterior fica esmaecido (`opacity-60`) e `aria-busy`, como a tabela de posições. Junto do percentual do cabeçalho, um `InfoTip` diz que é retorno ponderado no tempo e por que difere do resultado: dinheiro colocado ou retirado não conta como ganho nem perda |
| `series-chart.tsx` | `SeriesChart`, a linha em SVG decorativo (`aria-hidden`) de uma série de strings decimais, com área sob ela no tom dado — `primary`, `positive` ou `negative` —, linha de base tracejada opcional e, sob o ponteiro, a guia vertical, o marcador e a leitura que o chamador monta, do lado oposto ao ponto para não sair do card. O índice sob o ponteiro fica com o chamador, que o lê também fora do desenho; o toque marca o ponto, o arrasto horizontal percorre a série (`touch-pan-y`) e o vertical rola a página. Os números viram coordenadas só para desenhar, e os caminhos só se refazem quando a série muda. Serve a `PerformanceChart` e ao gráfico de preço do detalhe do ativo |
| `date-picker.tsx` | `DatePicker`, o seletor de data da shadcn/ui: um botão com o dia escolhido, ou "Pick a date", que abre o `Calendar` num `Popover`, com mês e ano em listas (`captionLayout="dropdown"`, de 100 anos atrás ao fim do ano corrente, o padrão da v10). As listas são o `Select` do design system, não o `<select>` nativo, cuja lista aberta o navegador desenha com as próprias cores, fora do tema: `Calendar` troca `MonthsDropdown` e `YearsDropdown` e navega por `goToMonth` do contexto do DayPicker a partir do mês exibido, por isso mostra um mês por vez (`numberOfMonths` só aceita 1); o mês aparece abreviado, para as duas listas caberem entre os botões de mês anterior e seguinte. A `nav` do DayPicker se estende pela linha do título, sobre as duas listas, e não recebe o ponteiro (`pointer-events-none`), só as setas: um clique em qualquer ponto do mês ou do ano, no texto ou no ícone, abre a lista, que acende no hover com o ícone. Abrir uma lista dentro do calendário não o fecha, e Esc fecha só a lista. Guarda o dia como `YYYY-MM-DD`; escolher fecha o calendário e devolve o foco ao botão, e reescolher o mesmo dia não o desmarca (`required`). O rótulo nomeia o botão no lugar do conteúdo, então o dia exibido também o descreve (`aria-describedby`), e o leitor de tela anuncia a data escolhida. Abrir o calendário leva o foco ao dia escolhido, como no diálogo de data do APG |
| `money-input.tsx` | `MoneyInput`, o campo de valor em que os dígitos entram pela direita, como num caixa eletrônico: 1, 2, 5 e 0 leem 12.50, apagar devolve o último dígito, e ponto ou vírgula digitados não mudam nada, porque a vírgula é implícita e nenhuma convenção de locale é adivinhada. Exibe agrupado no locale do app (`formatDecimal`), alinhado à esquerda, logo depois do símbolo da moeda, e com algarismos tabulares, enquanto o formulário guarda a string decimal que a API recebe. As casas vêm de `decimals`; um valor gravado ou colado com mais casas as mantém, até as 18 da coluna, e apagar tudo volta às casas do campo. Colar lê o valor inteiro por `readDecimal`, com qualquer das duas convenções e sem o símbolo ou código de moeda em volta; colagem ambígua ou ilegível não altera o campo e vira erro nele. `inputMode="numeric"`, até 20 dígitos inteiros |
| `transactions-table.tsx` | `TransactionsTable` lista as transações recebidas, com a coluna de ativo opcional, omitida no detalhe do ativo. Dia da execução no fuso do navegador, sem hora, o mesmo em que o formulário o registra. Cada linha tem um botão "Details", cujo nome acessível inclui tipo, ativo e data, que abre `TransactionDetailsDialog`. Editar e excluir partem desse diálogo e abrem por cima dele `TransactionFormDialog` ou `ConfirmDeletionDialog`: cancelar volta ao detalhe, e concluir fecha os dois. A escrita invalida a carteira por `useRefreshPortfolio`, e transações, posição, indicadores e performance buscam de novo |
| `transaction-details-dialog.tsx` | `TransactionDetailsDialog` mostra a transação inteira num diálogo, enquanto o web não tem tela de detalhe: tipo e ativo no título, data de execução na descrição, e quantidade, preço unitário, taxas, impostos, corretora e notas numa lista de definição, com o rótulo do preço conforme o tipo. Corretora e notas ausentes aparecem como "Not available"; nenhum total é calculado no web. O rodapé tem "Delete", "Close" e "Edit" |
| `transaction-form-dialog.tsx` | `TransactionFormDialog` cria ou edita uma transação: a criação parte do detalhe do ativo e do menu da linha na tabela de posições, sempre no contexto de um ativo, e a edição, do diálogo de detalhes. O formulário é montado a cada abertura. Em duas colunas a partir de `sm`, num diálogo mais largo que os outros (`max-w-xl`): o tipo em largura cheia, o preço e a quantidade lado a lado, com as linhas de rótulo na altura da ação `xxs` para os dois campos começarem na mesma altura com ou sem "Use 100 held", e o dia em largura cheia. Tipo em botões de rádio nativos, compra, venda, proventos e bonificação, em três colunas no celular, com uma dica que diz o efeito do tipo escolhido na posição e o que informar; quantidade em texto com teclado decimal, que começa em 1 na criação e descarta o sinal de menos digitado ou colado, seguida de botões que tiram e somam uma unidade inteira e preservam a fração, para toda classe (`shiftByWholeUnits`), com nome acessível ("Decrease quantity by 1"), `aria-controls` no campo e `aria-disabled` em vez de `disabled`, para o foco não cair do botão, quando o resultado não seria positivo ou o texto não é um número (vazio conta como zero); na criação, em venda e proventos, que contam unidades já detidas, e com a posição detendo alguma, "Use 100 held" ao lado do rótulo preenche a quantidade detida e devolve o foco ao campo; preço unitário, taxas e impostos em `MoneyInput`, com o rótulo do preço conforme o tipo (preço unitário, valor por unidade do provento ou custo atribuído da bonificação), com o código da moeda no rótulo e o símbolo dela dentro do campo de valor, a base da carteira na criação e a da transação na edição; na criação, o preço unitário começa no preço de mercado da posição, na moeda base, arredondado às casas do campo (exato se o arredondamento zerasse), com a dica "Market price as of" e a hora da cotação enquanto o valor for esse, e segue o tipo enquanto ninguém o altera: fica em compra e venda e esvazia em proventos e bonificação, cujo valor por unidade não é o de mercado; sem preço de mercado, ou com a posição em outra moeda, o campo começa vazio; dia da execução em `DatePicker`, sem hora, no fuso do navegador, o dia em que o diálogo abre na criação, enviado como o instante que `instantOfCalendarDay` ancora a ele, e o erro da API sobre `executedAt` aparece no dia; taxas, impostos, corretora e notas são detalhes: o formulário abre só com tipo, quantidade, preço e dia, e um grupo "Optional details" oferece um botão por detalhe ausente ("Add fees", com "Add" só para o leitor de tela), que revela o campo e o foca; cada detalhe revelado tem "Remove" ao lado do rótulo (nome acessível "Remove fees"), que descarta o valor e devolve o foco ao botão que o traz de volta. Na edição, abrem já revelados os detalhes com valor — taxa ou imposto diferente de zero, corretora ou notas presentes —, e escolher JCP revela os impostos, porque o JCP é pago líquido do imposto retido. O diálogo lê a posição (`usePosition`) antes de montar o formulário, com um esqueleto enquanto carrega, porque a classe do instrumento define as casas do preço unitário: as da moeda ou as de `UNIT_PRICE_DECIMALS` (8 em cripto e fundo), o que for maior; taxas e impostos usam as da moeda. Sem a posição, o formulário abre com as casas da moeda. Valida com os limites da API: a quantidade é lida por `readDecimal` (`lib/decimal.ts`), com ponto ou vírgula decimal, porque o teclado decimal em pt-BR só oferece a vírgula, e com separador de milhar de uma ou outra convenção quando os dois aparecem ou o mesmo se repete entre grupos de três; vírgula antes de exatamente três dígitos, como `1,500`, é recusada como ambígua, com as duas leituras na mensagem; até 20 dígitos inteiros e 18 casas, quantidade e preço maiores que zero, preço zero aceito só em bonificação, taxas e impostos em branco ou removidos como zero, corretora até 60 e notas até 500 caracteres, em branco como `null`. Na edição, a data que não foi alterada segue a gravada, com os milissegundos, para não mudar a ordem do razão. O botão principal nomeia o que a criação registra, conforme o tipo ("Add buy", "Add sell", "Add dividend", "Add JCP", "Add interest", "Add bonus"), e "Save changes" na edição. Enquanto envia, campos e botões ficam desabilitados e o diálogo não fecha; o erro da API aparece no campo que o `path` do detalhe indica ou, sem campo correspondente, num alerta, e o que foi digitado continua no formulário |
| `confirm-deletion-dialog.tsx` | `ConfirmDeletionDialog`, a confirmação de toda exclusão — ativo, carteira e transação — num `AlertDialog` com título, consequência, "Cancel" e o confirmar destrutivo com o nome da ação. Fica aberto até a exclusão terminar: enquanto ela corre, "Cancel", `Esc` e o clique fora não fecham, e o confirmar fica `aria-disabled` com spinner, sem perder o foco. A recusa da API, como a de excluir um `BUY` do qual um `SELL` depende ou a da última carteira, aparece num alerta no próprio diálogo, sem toast. Com `confirmationPhrase`, que só a exclusão de carteira passa, o diálogo pede que o nome seja digitado: o foco abre no campo, rotulado "Type <nome> to confirm" com o nome em `whitespace-pre-wrap` para que espaços repetidos apareçam, sem autocorreção nem autocompletar; o confirmar fica `aria-disabled` até o texto bater exatamente com o nome, comparados em NFC para que um acento decomposto não pareça divergir; `Enter` no campo ou no confirmar antes disso marca o campo inválido com "This does not match the name above" e devolve o foco a ele; durante a exclusão, o campo fica `readOnly`. Quem chama fecha o diálogo depois de excluir e leva o foco a um destino que continua na tela, já que a linha que o abriu saiu |
| `add-asset-dialog.tsx` | `AddAssetDialog`. Uma só visão de busca, sem lista antes de o usuário digitar. O termo, validado pelo mesmo padrão da API, vai a `GET /v1/instruments/search` 500 ms depois da última tecla, ou na hora com Enter, e a resposta vira uma lista de rádios nativos com os instrumentos que o usuário já vê, com o selo "Private" nos dele, e o que o provedor de cotação lista sob o termo como ticker, com o selo "New", mercado e moeda. Nome, classe e setor não são perguntados: escolher uma listagem envia só `symbol` e `listing { market, currency }`, e a API registra o resto a partir do provedor. Resultado único é escolhido sozinho quando a busca assenta, então Enter busca e um segundo Enter adiciona. O estado é anunciado (`role="status"`): buscando, quantos achados, e nenhum resultado com a dica de buscar pelo ticker; provedor indisponível avisa que faltam os instrumentos novos, com nova tentativa. Falha da busca, inclusive `429`, mostra erro com nova tentativa; falha da adição fica no diálogo (`role="alert"`), com o `409` de instrumento já visto traduzido para o ativo já existir na carteira, sem toast. Adicionar resolve a promessa de `onConfirm`, e quem hospeda o diálogo o fecha |
| `asset-dialogs.tsx` | `AssetDialogs`, o diálogo de ativo aberto numa tela — adicionar ativo, nova transação, em `TransactionFormDialog`, ou excluir ativo, em `ConfirmDeletionDialog` —, um por vez, descrito por `AssetDialog`, união discriminada por `kind` que a tela guarda em `useState` e troca por `onDialogChange`. Nenhum diálogo é aberto pela URL: com a URL e o estado dizendo ambos se o diálogo estava aberto, o `replaceState` do fechamento chegava a `useSearchParams` depois do render que o fechou, e o efeito que lia `action` o reabria, então "Cancel" só fechava no segundo clique. Adicionar ativo fecha o diálogo assim que a API confirma, e o toast "Asset added" oferece "Add transaction", que abre a nova transação do ativo na mesma tela, sem navegar. Usado pela tela de ativos e pela Overview |
| `detail-item.tsx` | `DetailItem`, termo e valor de uma lista de definição, usado pelo diálogo de transação e pelo detalhe do ativo |

## Feature components — `app/(protected)/assets/[symbol]`

O detalhe do ativo, em `/assets/[symbol]`, abre pelo símbolo nas posições da Overview e na tabela de posições. Nenhum valor é calculado no web.

| Componente | Responsabilidade |
| --- | --- |
| `[symbol]/page.tsx` | Página de servidor que repassa o símbolo da rota a `AssetDetail` |
| `asset-detail.tsx` | `AssetDetail` lê a carteira ativa, `GET /v1/portfolio/positions/:symbol` por `usePosition` e, em paralelo, `GET /v1/portfolio/positions/:symbol/indicators` por `usePositionIndicators`. Cabeçalho com retorno à tela de ativos, símbolo em `h1`, nome e o botão "Add transaction", com o ícone `Plus` como os botões de ação dos cabeçalhos de ativos e de carteiras, que abre `TransactionFormDialog` na moeda base da carteira. Em largura cheia, **Market**, na moeda da cotação: o preço em destaque, a variação do dia e o fechamento anterior, o horário da cotação em `<time>`; abaixo, a variação de `1M`, `3M`, `6M`, `YTD` e `1Y` em células com `Trend`, que são botões de rádio nativos no grupo "Price change" e escolhem a janela do gráfico de preço logo abaixo, em largura cheia: `SeriesChart` com os fechamentos diários de `closes` a partir do `openedOn` da janela, a abertura tracejada, a cor pelo sinal da variação, a leitura de dia e fechamento sob o ponteiro, e o primeiro e o último dia na legenda, que o leitor de tela lê com os dois fechamentos. Abre na janela mais longa, `1Y` quando o histórico a alcança; janela sem variação fica desabilitada, e sem nenhuma o gráfico mostra todos os fechamentos, sem seleção. É linha, não candles, porque o histórico guarda um fechamento por dia, sem abertura, máxima e mínima do pregão. Ao lado das janelas em telas largas, e abaixo do gráfico nas estreitas, a faixa de 52 semanas numa barra com o último fechamento marcado entre a mínima e a máxima; por fim a legenda com esse fechamento e o seu dia; por fim o perfil, com classe e só o mercado, a moeda e o setor que o ativo tem. Sem fechamento no último ano, o bloco de preço some; carregando, os esqueletos das janelas e do gráfico; em erro, a mensagem com nova tentativa, sem esconder a cotação. Abaixo, lado a lado em telas largas, **Position**, na moeda base, com o valor de mercado em destaque, o resultado não realizado com percentual, a quantidade, o preço médio, o preço e a alocação, e **Returns**, sobre `QuerySection`, na moeda do razão: o resultado realizado em destaque, a renda recebida, a dos últimos 12 meses, o `yield on cost`, cuja conta um `InfoTip` junto do rótulo explica, e o dia da primeira transação. A renda segue a classe: juros para títulos e Tesouro, renda para ações, ETFs, fundos e REITs, e, em cripto, caixa e outros, só quando o razão a registrou. Sem transação, Returns diz que começa na primeira. Em seguida, a performance da posição e as transações do ativo. Carregando, erro com nova tentativa para a carteira e para a posição, carteira ausente e ativo fora da carteira, com atalho para a tela de ativos; valor ausente aparece como "Not available". A barra da faixa converte preço em número só para posicionar o marcador; todo valor exibido sai da string decimal. Os destaques usam `HEADLINE_VALUE_CLASS` e `PROMINENT_VALUE_CLASS` de `components/financial.tsx`, os mesmos do valor da carteira na visão geral |
| `asset-transactions.tsx` | `AssetTransactions`, sobre `QuerySection`, lista as transações do ativo por `GET /v1/transactions` com `symbol`, 10 por página, com a página em `transactionsPage` na URL, em `TransactionsTable` sem a coluna de ativo, com a página anterior exibida enquanto a próxima carrega e navegação anterior e próxima com a página atual anunciada (`aria-live`). A página que ficou além da última depois de uma exclusão oferece ir para a última |

## Feature components — `app/(protected)/assets/_components`

| Componente | Linhas | Avaliação |
| --- | --- | --- |
| `positions-table.tsx` | 934 | Substituiu `assets-table.tsx`, que buscava só na página carregada. Lê `GET /v1/portfolio/positions` por `usePositions`, com busca, filtros, ordenação e paginação no servidor, sobre a carteira inteira, e o estado da listagem na URL. Uma barra só, sem rótulos visíveis (`Label` `sr-only`): busca por símbolo ou nome, enviada 300 ms depois da última tecla; classe ("All classes") e situação ("All positions", "Open positions", "Closed positions") em selects compactos que se destacam no tom primário quando filtram; "Clear" quando há busca ou filtro; e, à direita a partir de `lg`, "Sort by" com os oito campos da API e um botão de ordem (`aria-pressed`, "Descending order"), que alcança os campos sem coluna própria e a ordenação no celular. Sete colunas, com ordenação em botões no cabeçalho com `aria-sort`, cujo ícone só aparece na coluna ordenada ou sob hover e foco: ativo (símbolo, cujo link cobre a célula e leva ao detalhe, e nome), quantidade ("Qty"), custo médio ("Avg. cost"), preço, valor, alocação e lucro ou prejuízo com o percentual, os números alinhados à direita em algarismos tabulares, na moeda base e sem cálculo no web. Saem de cima para baixo — quantidade e custo médio abaixo de `xl`, quando voltam como linha secundária sob o valor e o preço; preço abaixo de `lg`; alocação abaixo de `md`; lucro ou prejuízo abaixo de `sm`, repetido então sob o nome —, e o detalhe do ativo guarda o resto. 10, 25 ou 50 linhas por página, com a página anterior esmaecida enquanto a nova carrega. Carregando, um esqueleto de 10 linhas no formato das colunas visíveis; erro com nova tentativa, vazio com atalho para adicionar ativo e sem resultado com atalho para limpar busca e filtros, e o `StaleState` acima da tabela quando a nova busca falha. "Refresh" é `RefreshPortfolioButton`. Menu de ações por linha: "View details", "New transaction" e, separada, "Delete". Sem TanStack Table: ordenação, filtro e paginação são do servidor, então os modelos de linha dela ficariam sem uso, a página tem no máximo 50 linhas, sem seleção, redimensionamento ou virtualização, e um vetor tipado de colunas basta; ela passa a valer com seleção em lote, visibilidade de colunas persistida, redimensionamento ou volume grande ordenado no cliente |
| `assets/page.tsx` | 107 | **Reescrita.** Cabeçalho com o nome e a moeda base da carteira e o botão de adicionar ativo; carregando, erro com nova tentativa e carteira ausente como na Overview. Os diálogos de adicionar ativo, nova transação e excluir ativo são os de `AssetDialogs`, abertos pelo cabeçalho e pelas ações da tabela. Depois de excluir, o foco vai para "Add asset" |

## Feature components — `app/(protected)/portfolios`

| Componente | Responsabilidade |
| --- | --- |
| `portfolios/page.tsx` | Tela de carteiras, sobre `QuerySection`, com `GET /v1/portfolios` 10 por página, com a página em `page` na URL: nome, moeda base e a marca da carteira ativa em cada linha, com "Use", "Edit" e "Delete", cujos nomes acessíveis incluem o da carteira. "Use" guarda a escolha da carteira ativa, e o bloqueio do armazenamento pelo navegador vira toast de erro. Com uma carteira só, "Delete" fica `aria-disabled`, focável e descrito pela regra, que fecha o cartão da lista como `InfoNote` sobre `--muted`. Carregando, erro com nova tentativa, vazio com atalho para criar e página além da última com atalho para ela. A exclusão pede o nome da carteira digitado, com a consequência e o aviso em destaque de que é permanente. Depois de excluir, o toast de sucesso traz a mensagem da API e o foco vai para "New portfolio", porque a linha que abriu o diálogo saiu |
| `portfolio-form-dialog.tsx` | `PortfolioFormDialog` cria ou edita: nome pela regra de `lib/portfolio-schema.ts`, e moeda base em botões de rádio nativos, com a atual incluída na edição mesmo fora das oferecidas. Na edição, avisa que a moeda só muda sem transações, e "Save changes" só habilita com alteração. Erro da API vai para o campo do `path` ou para um alerta no diálogo, como o 422 da moeda travada |

## Feature components — `app/(protected)/account`

| Componente | Responsabilidade |
| --- | --- |
| `account/page.tsx` | Tela de conta: perfil e segurança, cada um numa seção com `h2`. A descrição da segurança diz só o que a seção guarda; a consequência da troca fica em `PasswordForm`, junto do botão. O perfil carrega com `LoadingState` e falha com `ErrorState`; a segurança não depende do perfil. "Sign out" no cabeçalho só abaixo de `sm`, onde o menu lateral não o mostra, `aria-disabled` enquanto sai, como o do menu |
| `profile-form.tsx` | `ProfileForm` edita o nome, com a regra do sign-up, e mostra o e-mail, que a API não altera, como texto numa lista de definição. Salvar é otimista: o nome novo aparece na hora, no menu lateral inclusive, e a resposta do `PATCH`, que traz o perfil salvo, substitui o cache sem novo `GET /api/v1/user`. A falha devolve o nome anterior, revalida o perfil, porque um `PATCH` que estourou o tempo pode ter sido aplicado, e aparece no formulário, como antes. Confirma com o toast da API |
| `password-form.tsx` | `PasswordForm` troca a senha: atual, nova com a dica da política e confirmação. Senha atual errada, o `400` sem `details` da API, vai para o campo dela com foco; o resto segue `presentSubmitError`. No rodapé, `InfoNote` avisa que a troca encerra todas as sessões, inclusive a atual, e descreve o botão por `aria-describedby`; abaixo de `sm`, a nota fica acima do botão, que ocupa a largura. Aceita a troca, o proxy já apagou o cookie, e o web limpa o cache e volta ao sign-in |

## Forms

Padrão consistente e adequado: `react-hook-form` + `zodResolver`, schema Zod co-localizado com o diálogo. **Preservar o padrão.**

`TransactionFormDialog` é a referência: liga rótulo, `aria-invalid` e `aria-describedby` a cada campo, leva o erro da API ao campo do `path` do detalhe, ou a um alerta quando nenhum campo corresponde, e não limpa o que foi digitado.

`PortfolioFormDialog` segue a mesma referência, com a dica e o erro da moeda base ligados ao grupo de opções por `aria-describedby`.

**`components/form-field.tsx` — `FormField` e `ChoiceField`.** Extraem a fiação de rótulo/dica/erro que os dois formulários acima faziam à mão. `FormField` gera o `id` do controle, liga `Label`, `aria-invalid` e `aria-describedby` (dica e erro, cada um só quando presente; a dica aceita um nó, como a lista de requisitos da senha) e repassa isso como `FieldControlProps` para o `children` renderizar o `input`; o erro entra com `APPEAR_CLASS`; `isOptional` acrescenta "(optional)" ao rótulo, e `action` põe um controle ao lado dele, como o "Remove" dos detalhes da transação, sem mudar o rótulo dos campos que não o usam. `ChoiceField` é o mesmo contrato para um grupo do `SegmentedControl`, sobre um `fieldset`/`legend`: marca `data-invalid` no `fieldset` quando há erro, o que o `SegmentedControl` lê para colorir a borda. Consumidos por todo formulário: transação, carteira, cadastro de instrumento, sign-in, sign-up, perfil e senha.

Lacuna fechada: erro de servidor sem campo correspondente, e `aria-invalid`/`aria-describedby` fora dos dois formulários de referência (TASK 14.5) — `instrument-registration.tsx` mapeava `details[].path` da API para o campo do formulário (com o prefixo `instrument.` removido) e caía num alerta quando não havia campo correspondente, como os dois já faziam. O formulário saiu depois, com o cadastro manual de instrumento.

**`lib/submit-error.ts` — `presentSubmitError`.** O mapeamento de `details[].path` para o campo, com foco no primeiro, e o alerta `root.server` para o que não tem campo, antes copiado em três formulários. Cada formulário diz só como um `path` da API vira um campo seu, por `fieldOf`, e trata antes dele o erro que tem destino próprio, como o `409` do sign-up no e-mail ou a senha atual errada.

**`lib/account-schema.ts`.** E-mail, nome e política de nova senha, espelho da API menos a lista de senhas comuns, compartilhados pelo sign-in, pelo sign-up e pela tela de conta.

**`lib/portfolio-schema.ts`.** O nome de carteira, de 1 a 60 caracteres sem espaço nas pontas, espelho de `CreatePortfolioSchema`, compartilhado pelo diálogo de carteira e pela etapa de carteira do sign-up.

**`app/(public)/sign-up/_components/sign-up-form.tsx` — `SignUpForm`.** Três etapas num só formulário: "Your details" (nome e e-mail), "Security" (senha e confirmação, com o e-mail num `input` oculto `autoComplete="username"` para o gerenciador de senha) e "Portfolio" (nome da primeira carteira, "Main" por padrão, e moeda base em `SegmentedControl`, BRL por padrão). Enter e "Continue" enviam o formulário; fora da última etapa, o envio só valida os campos da etapa, por `trigger`, e avança com foco no primeiro campo da seguinte. "Back" volta sem validar, e nenhum valor se perde, porque os campos desmontados continuam registrados. Erro que cai em campo de outra etapa — o `409` no e-mail, um `path` da API ou uma regra entre etapas — traz a etapa dele de volta com o campo focado. O cabeçalho da etapa, em `aria-live`, diz o título e "Step N of 3", e a barra de progresso (`role="progressbar"`, 1 a 3) é um trilho `--muted` com o preenchimento `--primary` deslocado por `translateX` em 500 ms, sem transição sob `prefers-reduced-motion`. Os campos de cada etapa trocam por `SlideTransition`: avançar traz a etapa pela direita, voltar, pela esquerda, e o formulário acompanha a altura da nova etapa. `goToStep` grava a direção e a etapa no mesmo `flushSync`, porque o foco vai ao primeiro campo da etapa logo em seguida. O payload leva `portfolioName`, que a API usa no lugar de "Main".

**`components/password-requirements.tsx` — `PasswordRequirements`.** A dica da nova senha no sign-up e na tela de conta: o mínimo e a regra do e-mail, cada um marcado quando cumprido enquanto se digita, com o estado em texto para leitor de tela. Senha vazia não cumpre nenhum.

Todo formulário valida em `onChange`: o campo só mostra erro depois de receber um valor, e passar por ele sem digitar não o acusa; o envio valida todos. Onde o erro de um campo depende de outro — a confirmação, da senha; a senha, do e-mail no sign-up —, mudar o outro revalida o dependente se ele já tiver valor. Nenhum formulário desabilita os campos durante o envio; o erro da API aparece no próprio formulário, e toast só confirma sucesso.

## Data fetching

| Item | Avaliação |
| --- | --- |
| `lib/axios/axios.ts` | **Preservar.** Duas instâncias — `proxyApi` (browser, `/api`) e `serverApi` (server, `API_URL`) — resolvidas por `api.getInstance()`. Interceptor de 401 apaga o token por `/v1/session/expire` e redireciona, com o aviso de expiração só se o usuário não encerrou a sessão |
| `lib/axios/errors.ts` | **Preservar.** `ApiProxyError` normaliza o erro do backend, e `isNotFoundError` reconhece o 404 pelo `code` do corpo, que chega ao navegador pelo proxy |
| `lib/api-proxy.ts` | **Preservar.** `forwardToApi` encaminha ao backend com o `Bearer` do cookie, repassa status e corpo, e concentra o `try/catch` dos proxies; `jsonPayload` responde `400` a corpo que não é JSON |
| `lib/react-query.ts` | **Preservar.** Defaults auditados na TASK 16.1 e justificados em `docs/data-fetching.md`: dado fresco por 60 s, sem refetch ao focar a janela, e repetição até duas vezes que nunca alcança requisição rejeitada com 4xx. Mutation segue o padrão do TanStack Query e não repete, para que uma escrita cujo tempo esgotou não seja gravada de novo e o erro chegue ao formulário na primeira resposta |
| `app/api/v1/*/types.ts` | **Preservar o padrão** de tipos co-localizados por rota |

**Hooks de domínio:** componente não conhece URL, Axios nem query key. `hooks/use-portfolio.ts` (carteira ativa, lista, criação, edição e exclusão de carteira, visão geral, posições, posição por símbolo, indicadores da posição, alocação e performance; as posições mantêm a página anterior enquanto a pedida, com outra ordenação, busca ou filtro, carrega, e nenhuma repete a busca que respondeu 404, pela regra geral de `retry`), `hooks/use-assets.ts` (criação e remoção de ativo), `hooks/use-transactions.ts` (listagem, que mantém a página anterior enquanto a pedida carrega, criação, edição e exclusão) e `hooks/use-user.ts` (perfil, sign-in, sign-up e sign-out) montam a query ou a mutation sobre os proxies de `app/api/v1`. Hook escopado por carteira recebe a carteira e não dispara a requisição sem ela (`skipToken`) por `usePortfolioScopedQuery`, e toda mutação de escrita anuncia o sucesso e invalida o escopo por `useAnnouncePortfolioChange`, sem esperar a revalidação, para o diálogo fechar assim que a API confirma, ambos em `hooks/use-portfolio.ts`, com um toast de sucesso no canto inferior direito, que não toma o foco e só aparece depois da resposta de sucesso: a criação de ativo diz "Asset added" e em que carteira o símbolo entrou e, quando a tela oferece, traz a ação "Add transaction", com 10 s em vez dos 4 s do `sonner` e dispensada quando a tela que a anunciou sai, e a de transação diz o que registrou ("Buy recorded") e a quantidade, o símbolo, o preço unitário na moeda da transação e o dia; edição e exclusão repetem a mensagem da API. A falha de uma criação não abre toast: fica no diálogo, junto ao campo ou no alerta do formulário, com o que foi digitado preservado; mutation sem carteira falha, com toast nas de ativo e no diálogo que a disparou nas de transação, que não abrem toast de erro.

**Query keys e invalidação:** toda query key sai de `queryKeys`, em `lib/react-query.ts`, no formato `[raiz, ...escopo, recurso, parâmetros]`: `['user']` para o perfil, `['portfolios', 'page', página]` para a lista de carteiras, `['portfolios', 'details', portfolioId]` para a carteira escolhida e `['portfolio', portfolioId, recurso, parâmetros]` para o que pertence a uma carteira — `overview`, `positions`, `position`, `allocation`, `performance` e `transactions`, e `['portfolio', portfolioId, 'position', símbolo, 'indicators']` para os indicadores da posição, que a invalidação da posição alcança. Os parâmetros são os da requisição, nunca um valor derivado, como o horário de atualização de outra query. Toda escrita bem-sucedida numa carteira, e o "Refresh" da Overview e da tela de ativos invalidam `['portfolio', portfolioId]` por `useRefreshPortfolio`: as queries ativas do escopo buscam de novo, as inativas ficam obsoletas, e as desativadas por `skipToken` ficam de fora. O layout público descarta todo o cache ao montar, e um 401 de sessão recarrega a página em `/sign-in`; por isso as keys não levam o usuário (ver TD-023 sobre o `QueryClient` no servidor).

## State

| Item | Avaliação |
| --- | --- |
| `providers/app-provider.tsx` | **Preservar** — QueryClient, tema, toaster, progress bar, error boundary. O toaster é `components/toaster.tsx` |
| `hooks/use-user.ts` | **Preservar.** Perfil do chamador por `GET /api/v1/user` e mutations de sign-in, sign-up, sign-out e troca de senha; o cache da sessão anterior é descartado pelo layout público, depois que as telas protegidas desmontam |
| `hooks/use-page-param.ts` | `usePageParam`, a página de uma listagem num parâmetro da URL, com o nome dado por quem chama: `page` nas carteiras, `positionsPage` na Overview e `transactionsPage` no detalhe do ativo. Valor que não é página mostra a primeira, e a primeira sai da URL. A leitura do valor é `pageNumberFrom`, de `lib/pagination.ts`, a mesma da tabela de posições, que antes aceitava qualquer inteiro — `?page=1e300` chegava à API e voltava como erro |

**Fronteira servidor↔UI:** dado de servidor — carteira, visão geral, posições, alocação, performance, transações e perfil — vem só do React Query. A URL guarda o que um refresh ou um link precisa reproduzir: busca, filtros, ordenação, tamanho e página da tabela de posições, a página das outras listagens e o período do gráfico, com `updateUrlQuery`, que empilha, para Voltar desfazer a mudança. Diálogo não entra na URL. `useState` guarda o resto do estado de interface: o diálogo aberto e o símbolo sobre o qual age, a transação selecionada e a ação sobre ela e o texto digitado na busca. As tabelas exibem a paginação canônica da resposta e formatam os valores na moeda que a resposta informa.

## Utilities

| Item | Avaliação |
| --- | --- |
| `lib/utils.ts` → `cn()` | **Preservar** |
| `lib/pagination.ts` | **Novo.** `FIRST_PAGE`, a primeira página das listagens da API, antes redeclarada em seis arquivos, e `pageNumberFrom`, que lê a página de um parâmetro da URL: inteiro seguro a partir de 1, ou a primeira página |
| `lib/motion-features.ts` | **Novo.** O pacote de recursos do framer-motion (`domMax`, que inclui a animação de layout do indicador compartilhado), num módulo só dele para `MotionScope` carregá-lo por `import()` |
| `lib/motion.ts` | **Novo.** `TAB_SPRING`, a mola das abas (300 ms, rebote de 0,25); `TAB_PANEL_VARIANTS`, o painel que entra 32 px deslocado no sentido da navegação e sai no sentido oposto, com fade mais curto na saída que na entrada; `TAB_PANEL_HEIGHT_TRANSITION`, a mesma mola sem rebote, para o que fica abaixo do painel não quicar. `APPEAR_CLASS`, o fade de entrada de 200 ms do `tw-animate-css`, para o que substitui um carregamento ou responde a uma ação: o conteúdo de `QuerySection`, `EmptyState`, `ErrorState`, o erro de `FormField`, `AuthNotice`, o bloco de preço do detalhe do ativo e o gráfico de preço, que refaz o fade a cada janela escolhida. Nada entra animado só por decoração; sob `prefers-reduced-motion`, `globals.css` encurta toda animação |
| `lib/toast-lifetime.ts` | **Novo.** `TOAST_LIFETIME_MS`, os 4 s padrão do `sonner`, e `toastLifetimeOf`, que dá a um toast a `duration` e o `--toast-lifetime` da barra de contagem juntos, para que nunca divirjam |
| `common/utils.ts` → `formatNumber`, `formatMoney`, `formatPrice`, `formatUnitAmount`, `formatQuantity`, `formatPercent`, `formatDecimal`, `currencyFractionDigits`, `currencySymbolOf`, `signedValueTone` | **Promover a primitive.** Formatadores centrais, compartilhados pela tela de ativos, pelo detalhe do ativo e pela Overview: preço e quantidade com as casas decimais do valor recebido, valor de uma unidade com ao menos 4 algarismos significativos, para que o preço abaixo de um centavo não vire zero, percentual com duas casas, valor em casas fixas para o `MoneyInput`, as casas da unidade menor de cada moeda e a cor pelo sinal; viram base de `Money`/`Percentage` (TASK 12.2). `currencySymbolOf` tira do mesmo `Intl.NumberFormat` de `formatMoney` o símbolo que o campo de valor do formulário de transação mostra; a tabela `CURRENCIES`, que repetia à mão R$, $ e €, saiu, e uma carteira em outra moeda ganha o símbolo dela (£, ¥) em vez do código. As moedas oferecidas na criação de carteira e no sign-up são `OFFERED_CURRENCIES`, com `DEFAULT_CURRENCY` |
| `lib/dates.ts` | **Novo.** Todo o tratamento de datas do web, sobre Day.js com os plugins `utc` e `customParseFormat`. A API recebe e devolve instantes em ISO 8601 e os grava em UTC (`TIMESTAMP(3)`); o dia de uma série de performance é a sua meia-noite UTC. Aqui ficam a formatação — cotação, execução e dia de série, este em UTC —, a leitura de um instante como dia (`YYYY-MM-DD`) no fuso do navegador, e a volta: `instantOfCalendarDay` devolve, do dia escolhido, a mais tardia entre a meia-noite local e a meia-noite UTC, o primeiro instante que cai nesse dia nos dois fusos, porque a tabela mostra a execução no fuso do navegador e a série de performance a conta no seu dia UTC; qualquer deslocamento, de −12 a +14 horas, nomeia assim o mesmo dia nas duas leituras. `access-token.ts` segue com `Date`, porque converte o `exp` do JWT, um instante de protocolo, não uma data de calendário |
| `lib/decimal.ts` | **Novo.** Decimais exatos no web: os limites de `DECIMAL(38,18)`, `readDecimal` para o que a pessoa digita ou cola, com as duas convenções de separador e a vírgula ambígua recusada, e aritmética em unidades de `10^-escala` num `bigint` — arredondar para uma escala, somar unidades inteiras —, sem nunca passar por `number` |
| `common/utils.ts` → `truncateText`, `sanitizeInputValue` | **Preservar** |
| `common/utils.ts` → `updateUrlQuery` | **Preservar.** Escreve a query pela History API nativa, `pushState`, que o App Router sincroniza com `useSearchParams` desde o Next 14.1; substituiu `replaceUrl`, cujo conflito com o router a TASK 13.3 previa. `replaceUrlQuery`, que substituía a entrada ao abrir e fechar diálogo pela URL, saiu com ele |
| `common/constants.ts` | **Preservar e expandir** — rotas, com a do detalhe do ativo por símbolo, cookies, moedas, fontes, rótulos e tons dos tipos de transação e o rótulo do preço de cada tipo |
| `types/index.ts` | **Preservar** utilitários (`Maybe`, `WithId`, `Pagination`); **remover** `TailwindColors*`, acoplamento à paleta do Tailwind sem uso justificado |

## Duplicações relevantes

1. **Cálculo financeiro em três lugares** — `dominance`/`totalInvestedValue` no proxy (`api/v1/assets/route.ts`), preço médio em `asset-transaction-table-cell.tsx`, `investedValue` no backend. Uma única fonte de verdade na FASE 5. **Resolvido:** o componente de transação saiu junto com a listagem por símbolo, o proxy foi removido, e o web exibe os valores que o backend calcula, como a alocação e o resultado por posição de `GET /v1/portfolio/positions`.
2. **Bloco `try/catch` idêntico em todas as 9 rotas de proxy** — mesmas 4 linhas de leitura do cookie + mesmo `catch`. Candidato a um wrapper único.
3. **Bloco `catch` idêntico nos 16 controllers do backend** — resolvido por error handler global (TASK 1.6).
4. **Dois pacotes de ícones**: `react-icons` e `lucide-react`, ambos em uso nos mesmos arquivos (`add-asset-dialog.tsx`, `sidebar.tsx`, `app-provider.tsx`). **Resolvido:** a auditoria de bundle encontrou ainda um terceiro, `@radix-ui/react-icons`, nos primitives. Todos passaram para `lucide-react`, e os outros dois saíram de `package.json`. Ver `docs/performance.md`, §Bundle.
5. **Schema Zod duplicado entre front e back** — `add-asset-dialog.tsx` repete o padrão e o limite de 120 caracteres de `InstrumentSearchTermSchema` sem contrato compartilhado. O cadastro manual, que repetia os limites de `InstrumentAttributesSchema`, saiu: os atributos do instrumento vêm do provedor, no backend.

## Componentes excessivamente específicos

* `assets-table.tsx` — acoplado a `LimitPerPageOptions` e ao tipo `PageRequest` importados de `../page`, dependência circular de fato entre página e componente. **Resolvido:** a tabela de posições que o substituiu define as próprias opções e não importa nada da página.

## Candidatos a virar primitive

| Origem | Primitive de destino | Task |
| --- | --- | --- |
| `formatNumber` + `CURRENCIES` | `Money`, `Percentage` | 12.2 |
| cor pelo sinal em `signedValueTone` e `SignedAmount` de `components/amounts.tsx` | `ProfitLoss`, `Trend` | 12.1 / 12.2 |
| `Skeleton` em uso ad-hoc | `LoadingState` | 12.4 |
| `QuerySection` e `LoadErrorAlert` | `EmptyState`, `ErrorState`, `NoResultsState`, `StaleState` | 12.4 |
| cabeçalho repetido em `assets/page.tsx`, `account/page.tsx` e `(overview)/page.tsx` | `PageHeader`, `SectionHeader` | 12.3 |

## Resumo

**Preservar sem alteração relevante:** os primitives de `components/ui` que sobraram à TASK 20.3, camada axios, providers de app, padrão de formulários, padrão de tipos por rota.

**Refatorar:** `common/utils`. `sidebar` foi redesenhado — ver a entrada acima.

**Reescrever:** `assets-table` e `assets/page`, já reescritos; a tabela deu lugar a `positions-table`. `add-asset-dialog` também, hoje uma só visão de busca sobre os instrumentos vistos e as listagens do provedor.

**Criar:** primitives financeiras, componentes de estado de dados, camada de gráficos. Navegação mobile já existe, na mesma `nav` do `sidebar` — barra inferior abaixo de `sm`.
